#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;
typedef std::vector<int> vi;
typedef std::vector<vector<int>> vvi;
#define endl '\n'
#define pb push_back

int main()
{
    ios_base:: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cout<<ceil(3/(2.0))<<endl; // ceil(3/2) will give wrong answer
    cout<<ceil(3.0/2)<<endl;
    cout<<floor(3/2)<<endl;
    return 0;
}